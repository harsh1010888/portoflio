# Copy to .env and replace with your MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/yourDB?retryWrites=true&w=majority
PORT=5000
# Optional: restrict CORS origin (frontend). E.g. https://your-frontend-domain.com
CORS_ORIGIN=
